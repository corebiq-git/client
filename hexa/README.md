COREBIQ /hexa deployment package

Upload the CONTENTS of this folder into the server directory mapped to /hexa/.
The entry point is index.html.
Firebase project: corebic--inspirego.
The application modules/components were not redesigned.
