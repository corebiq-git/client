
// COREBIQ Firebase configuration
// Replace these placeholders with your Firebase Web App configuration.
// Database layer is intentionally centralized so every module can use the same backend.
const COREBIQ_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.firebasestorage.app",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Firebase SDK imports can be added here when deploying.
// Recommended collections:
// companies, users, customers, leads, contacts, deals, invoices, quotes,
// subscriptions, payments, expenses, bank_transactions, cash_transactions,
// cheques, employees, attendance, payroll, leave_requests, templates,
// compliance, documents, notifications, audit_logs, settings.
